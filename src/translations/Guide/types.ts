export type NavObj = {
    title: string;
    description: string;
    flag: string;
    flag_description: string;
    logo_description: string;
    nav_brand: string;
};

export type StartGameObj = {
    title: string;
    description: string;
};

export type GameSetupObj = {
    title: string;
    description: string;
    nameEnterImg: any;
    charSelectImg: any;
    nameEnterDescription: string;
    charSelectDescription: string;
};

export type PlayersObj = {
    title: string;
    description: string;
};

export type GodVisionObj = {
    title: string;
    description: string;
};

export type ScenariosObj = {
    title: string;
    description: string;
};
